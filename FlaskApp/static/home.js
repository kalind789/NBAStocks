// static/home.js

// Smooth scrolling for anchor links (if any)
$(document).ready(function() {
    // Animate on scroll initialization (if using AOS library)
    AOS.init({
        duration: 800,
        once: true
    });
});